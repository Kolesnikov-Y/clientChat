import { createPath } from "rd-url-utils";

export const REGISTER_PAGE_URL = createPath<{}>('/register'); 
export const LOGIN_PAGE_URL = createPath<{}>('/login'); 
export const StARTED_PAGE_URL = createPath<{}>('/startedPage'); 
export const FORGOT_PASSWORD_PAGE_URL = createPath<{}>('/forgotPassword'); 
